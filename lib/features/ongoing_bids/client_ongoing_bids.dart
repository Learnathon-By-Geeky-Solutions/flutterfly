import 'package:flutter/material.dart';

class ClientOngoingBids extends StatelessWidget {
  const ClientOngoingBids({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Client Ongoing Bids'),
    );
  }
}
import 'package:flutter/material.dart';

class MyRfq extends StatelessWidget {
  const MyRfq({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('My RFQ'),
    );
  }
}
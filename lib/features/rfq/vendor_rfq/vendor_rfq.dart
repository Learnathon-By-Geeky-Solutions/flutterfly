import 'package:flutter/material.dart';

class VendorRfq extends StatelessWidget {
  const VendorRfq({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Vendor Rfq'),
    );
  }
}
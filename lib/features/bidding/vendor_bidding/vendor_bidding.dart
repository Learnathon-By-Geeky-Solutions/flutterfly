import 'package:flutter/material.dart';

class VendorBidding extends StatelessWidget {
  const VendorBidding({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Vendor Bidding'),
    );
  }
}